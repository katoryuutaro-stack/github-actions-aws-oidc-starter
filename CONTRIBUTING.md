# Contributing

1. Create a feature branch.
2. Run `make validate`.
3. Keep AWS permissions least-privilege and resource-scoped.
4. Never include real credentials, private repository names, or customer account data in fixtures.
5. Open a pull request with the threat model and validation evidence.
